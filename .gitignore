__pycache__/
*.pyc
.env
.env.local
.venv/
venv/
.DS_Store

# ไฟล์ส่วนตัว ไม่เกี่ยวกับโปรเจกต์
resume-*.pdf
roleplay-agent-notes.md

# ไฟล์สถานะของเครื่องมือที่ใช้ทำงาน ไม่ใช่ส่วนหนึ่งของโปรเจกต์
.claude/
.vscode/
.idea/

# ข้อมูลนิยาย (ไฟล์ PDF ลิขสิทธิ์ + index/cache ที่ derive มาจากมัน — รวมกันเกิน 1GB)
# กับเซฟบทสนทนาที่เล่นจริง (runtime state เฉพาะเครื่อง ไม่ใช่ source code)
noveldata/
story_saves/
